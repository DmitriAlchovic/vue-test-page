export type ArticlePreview = {
    id: number;
    title: string;
    body: string;
    image: string;
    link: string,
    linkTitle: string,
}

export type ApplicationPreview = {
    id: number;
    name: string;
    link: string;
    image: string;
}
